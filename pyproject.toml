[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gait-analysis"
version = "0.1.0"

dependencies = [
    "numpy",
    "scipy",
    "matplotlib",
    "ezc3d",
]

[tool.setuptools.packages.find]
where = ["src"]